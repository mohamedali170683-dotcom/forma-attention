import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Forma & Attention - Behavioral Science Optimization Platform",
  description: "Engineering persuasion using proven behavioral science. Optimize your conversions with BSOS scoring.",
  keywords: ["behavioral science", "conversion optimization", "BSOS", "marketing"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
